# Progress

## Статус

- MVP реализован: Next.js App Router приложение, интерактивная 3D-сцена, локальные данные H-Ca, селектор элементов и информационная панель.
- Интерфейс переведен на shadcn/ui-compatible primitives и Tailwind CSS v4.
- Ошибочный ручной переключатель орбиталей удален; уровни переведены на структуру подуровней `s/p/d/f` с орбиталями и электронами.
- Облака вокруг электронов удалены; электроны двигаются по разным траекториям орбиталей `s/p/d/f`.
- `AtomScene` переделан на иерархию `уровень -> подуровень s/p/d/f -> орбиталь -> электрон`; орбитали больше не выглядят как случайные траектории, а отображаются как учебные формы `s/p/d/f`.
- `InfoPanel` показывает `spdf`-конфигурацию и список уровней с подуровнями, количеством орбиталей и электронов.
- По уточнению пользователя облака вероятности удалены: `s/p/d/f` теперь отображаются как траектории движения электронов.
- Исправлено смешивание уровней: `2p` и другие внешние `spdf`-траектории теперь строятся вокруг радиуса своего энергетического уровня и не проваливаются к `1s`.
- После уточнения формы `p`: `p` снова отображается как локальная восьмерочная траектория, но ее центр вынесен на радиус соответствующего уровня, чтобы не смешиваться с `1s`.
- После уточнения пользователя центр `s/p/d/f`-траекторий возвращен в центр атома; принадлежность уровню теперь задается масштабом траектории, а не смещением центра.
- Ориентация `p`-орбиталей исправлена: три `p`-траектории выровнены по осям `x/y/z`, а не разведены "снежинкой".
- `p`-орбитали разведены по трем ортогональным плоскостям: `px` в `xy`, `py` в `yz`, `pz` в `xz`.
- Масштаб `p`-траекторий увеличен: `2p` теперь визуально больше `2s` на том же энергетическом уровне.
- `.gitignore` расширен для исключения локальных артефактов Next.js/Bun/tooling/IDE/log/temp/env перед отправкой проекта на GitHub.
- Проверки `bun run format` и `bun run build` завершились успешно.

## Known Issues

- В `/home/jarlix/atom` не найден git-репозиторий, поэтому сравнение с последним коммитом недоступно.

## Changelog

- 2026-04-30: Создана базовая структура Memory Bank и зафиксирован scope MVP из `docs/PRD.md`.
- 2026-04-30: Создан `docs/README.md` как источник архитектурной правды.
- 2026-04-30: Реализован MVP: Next.js проект, React Three Fiber сцена, элементы H-Ca, селектор, инфопанель, темная адаптивная главная страница.
- 2026-04-30: Исправлены замечания Biome и подтверждена production сборка `bun run build`.
- 2026-04-30: Добавлены Tailwind CSS v4, `components.json`, shadcn/ui-compatible `Button`, `Card`, `Badge`; интерфейс главной страницы, селектор и инфопанель переведены на UI primitives.
- 2026-04-30: Добавлены `OrbitalSelector`, типы orbital view и 3D-режимы `shell`, `s`, `p`, `d` в `AtomScene`.
- 2026-04-30: Начата замена ручного переключателя орбиталей на автоматическое деление уровней на подуровни и орбитали с электронами.
- 2026-04-30: Удалены `OrbitalSelector` и ручной `OrbitalView`; добавлен `buildElectronLevels`, отображение подуровней `s/p/d/f`, орбиталей и электронов в `AtomScene`, конфигурация в `InfoPanel`.
- 2026-04-30: Орбитали перестали быть одинаковыми сферами: добавлены разные формы `s`, `p`, `d`, `f` в `AtomScene`.
- 2026-04-30: Удалены локальные облака орбиталей; `s` отображается как круговая траектория слоя, `p` как восьмерка, `d/f` как усложненные траектории с электронами на них.
- 2026-05-01: `AtomScene` переделан на иерархическое отображение энергетических уровней, подуровней `s/p/d/f`, отдельных орбиталей и электронов внутри орбиталей; `InfoPanel`, `docs/README.md` и Memory Bank синхронизированы.
- 2026-05-01: Удалены облака вероятности из `AtomScene`; `s/p/d/f`-орбитали отображаются как траектории движения электронов, `InfoPanel` и Memory Bank обновлены под терминологию пользователя.
- 2026-05-01: Исправлена геометрия `spdf`-траекторий: внешние орбитали центрируются на своем энергетическом уровне, чтобы `2p` визуально не смешивалась с `1s`.
- 2026-05-01: Скорректирована `p`-орбиталь: сохранена форма восьмерки, а привязка к уровню выполняется через смещение центра траектории.
- 2026-05-01: Центр `s/p/d/f`-орбиталей возвращен в ядро атома; разные уровни отличаются радиусом/масштабом траектории.
- 2026-05-01: Исправлена ориентация `p`-орбиталей: `px`, `py`, `pz` выстроены по осям `x/y/z`.
- 2026-05-01: `p`-орбитали перестроены без Euler-поворотов: каждая траектория рассчитывается в своей ортогональной плоскости.
- 2026-05-01: Увеличен главный радиус `p`-траекторий относительно `s`, чтобы `2p` не выглядела меньше `2s`.
- 2026-05-01: Обновлен `.gitignore`: добавлены исключения для тестовых отчетов, dist/cache/temp/log/IDE/tooling артефактов и разрешены `.env.example`/`.env.sample`.

## Контроль изменений

last_checked_commit: 2026-05-01-gitignore-update
